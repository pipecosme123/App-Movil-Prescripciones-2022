import React from "react";
import { StyleSheet, Text, View } from "react-native";

const BarNav = () => {
  return (
    <View className="BarNav">
      <Text> TITULO </Text>
    </View>
  );
};

const styles = StyleSheet.create({

});


export default BarNav;
